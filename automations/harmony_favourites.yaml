# - alias: Harmony Favourite Channel Selection
  # hide_entity: True
  # trigger:
    # - platform: state
      # entity_id: input_select.fav_chan
      # to: 'ABC 24'
    # - platform: state
      # entity_id: input_select.fav_chan
      # to: '9 Life'
    # - platform: state
      # entity_id: input_select.fav_chan
      # to: 'SBS Viceland'
  # action:
    # - service: remote.turn_on
      # entity_id: remote.living_room
      # data_template:
        # activity: >
          # {% if is_state_attr('remote.living_room', 'current_activity', 'Watch TV') %}
            # 21450129
          # {% else %}
            # 13915533
          # {% endif %}
    # - delay: >
        # {% if is_state("input_select.harmony", "Power Off") %}
          # 00:00:06
        # {% else %}
          # 00:00:01
        # {% endif %}
    # - service_template:
        # entity_id: remote.living_room
        # command: >
          # {% if is_state("input_select.fav_chan", "ABC 24") %}
            # [InputTv, 2, 4]
          # {% elif is_state("input_select.fav_chan", "9 Life") %}
            # [InputTv, 9, 4]
          # {% elif is_state("input_select.fav_chan", "SBS Viceland") %}
            # [InputTv, 3, 1]
          # {% else %}
            # InputDigital
          # {% endif %}
        # device: 37937380
    # - service: input_select.select_option
      # entity_id: input_select.fav_chan
      # data_template:
        # option: "Select Channel"